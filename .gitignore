# --- Sirlar ---
.env
.env.*
!.env.example
*.key
*.pem
secrets/

# --- Python ---
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
.ruff_cache/
.mypy_cache/
*.egg-info/
dist/
build/

# --- Node / frontend ---
node_modules/
web/dist/
web/.vite/
*.log

# --- Ma'lumotlar ---
*.db
*.sqlite3
data/raw/
data/tmp/
*.osm.pbf
*.geojson.tmp

# --- Sandbox qoldiqlari ---
# `/tmp` to'lib qolganda `tempfile` yozuvchanlik sinovini repo ichiga
# yozadi: 8 belgili tasodifiy nom, 4 bayt (`blat`), rejim 700. Agent
# ularni o'chira olmaydi (`Operation not permitted` mountda,
# `allow_cowork_file_delete` esa rejalashtirilgan runni to'xtatadi —
# CLAUDE.md §1). 👤 qo'lda o'chiring.
#
# Namuna bilan yozib bo'lmaydi: `sveta/????????` `sveta/Makefile` ni ham
# tutadi, ya'ni haqiqiy fayl jimgina commitdan tushib qolardi. Shuning
# uchun nomlar birma-bir sanaladi.
sveta/4wpi2gpv
sveta/4hs3xo8b
sveta/58pozfd9
sveta/klc5pety

# --- Muhit ---
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp
